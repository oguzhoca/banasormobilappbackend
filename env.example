# Google OAuth Configuration
GOOGLE_CLIENT_ID=your_google_client_id_here

# Server Configuration
PORT=3000

# Note: OpenAI API keys are now stored per user, not globally
# Users will provide their own API keys via /api/save-token endpoint
